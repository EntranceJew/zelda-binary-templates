struct {
    N64ROMHead makerom;

    FSeek( SAMPLES );
    SampleTable sampleTable;

    FSeek( INSTRUMENT );
    InstrumentSetTable instrumentSetTable;

    //FSeek( SEQTABLE );
    //SequenceTable sequenceTable;
} ROM;