typedef struct {
    uint start;
    uint end;
} ZFileSystemRange;

typedef struct {
    // the "v" means virtual, relative to the ROM
    uint vStart;
    uint vEnd;

    // the "p" means physical, relative to the N64 memory map
    uint pStart;
    uint pEnd;
} ZFileSystemEntry;